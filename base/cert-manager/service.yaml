apiVersion: v1
kind: Service
metadata:
  name: cert-manager
  namespace: cert-manager
  labels:
    app.kubernetes.io/name: cert-manager
    app.kubernetes.io/part-of: ninekube
spec:
  selector:
    app.kubernetes.io/name: cert-manager
  ports:
    - port: 9402
      targetPort: 9402
