apiVersion: v1
kind: Service
metadata:
  name: collabora
  namespace: nine
  labels:
    app.kubernetes.io/name: collabora
    app.kubernetes.io/part-of: ninekube
spec:
  type: ClusterIP
  ports:
    - port: 9980
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: collabora
